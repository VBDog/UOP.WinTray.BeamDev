<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                   >
    <Canvas x:Key="Background2">
        <Path Data="M0,50 C0,50 250,50 250,50 201,50 192,6 175,6 175,6 12,6 12,6 4,6 0,10 0,12 " Stretch="Fill">
            <Path.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <LinearGradientBrush.GradientStops>
                        <GradientStopCollection>
                            <GradientStop Offset="0" Color="#FFCBCBCB" />
                            <GradientStop Offset="0.951" Color="WhiteSmoke" />
                        </GradientStopCollection>
                    </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>
            </Path.Fill>
        </Path>
    </Canvas>

    <Canvas x:Key="Background3">
        <Path Data="M0,0 C0,0 0,226 0,226 0,201 40,192 40,175 40,175 40,40 40,12 40,4 38,0 32,0" Stretch="Fill">
            <Path.Fill>
                <LinearGradientBrush StartPoint="0,1" EndPoint="1,1">
                    <LinearGradientBrush.GradientStops>
                        <GradientStopCollection>
                            <GradientStop Offset="0" Color="#FFCBCBCB" />
                            <GradientStop Offset="0.951" Color="WhiteSmoke" />
                        </GradientStopCollection>
                    </LinearGradientBrush.GradientStops>
                </LinearGradientBrush>
            </Path.Fill>
        </Path>
    </Canvas>

    <Style TargetType="{x:Type Border}" x:Key="InnerBorder1">
        <Setter Property="Background" Value="WhiteSmoke"/>
        <Setter Property="BorderBrush" Value="Gray"    />
        <Setter Property="BorderThickness" Value="0 1 1 1" />
        <Setter Property="CornerRadius" Value="0,20,20,0" />
        <!--<Setter Property="Border.Effect">
            <Setter.Value >
                <DropShadowEffect ShadowDepth="0" Color="Green" BlurRadius="5" />
            </Setter.Value>
        </Setter>-->
    </Style>

    <Style x:Key="ExpanderRightToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Style="{StaticResource InnerBorder1}" Height="Auto" HorizontalAlignment="Left" 
                            VerticalAlignment="Top" Margin="0,0,2,0" Padding="0 10 0 10 " SnapsToDevicePixels="True">
                        <!--<Border.Background>
                            <VisualBrush Stretch="Uniform" Visual="{StaticResource Background3}" />
                        </Border.Background>-->
                        <Grid >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition  Height="*" />
                            </Grid.RowDefinitions>
                            <Image x:Name="arrow" Grid.Row="0" Width="14" Margin="0 2 2 2"  />
                            <ContentPresenter x:Name="ContentPresenter" Grid.Row="1" Margin="0,4,2,2" RecognizesAccessKey="True" SnapsToDevicePixels="True" >
                                <ContentPresenter.LayoutTransform>
                                    <RotateTransform Angle="90" CenterX="0" CenterY="0" />
                                </ContentPresenter.LayoutTransform>
                            </ContentPresenter>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExplorerExpander" TargetType="{x:Type Expander}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3,3,1,3" SnapsToDevicePixels="true">
                        <DockPanel>
                            <ToggleButton HorizontalAlignment="Right" VerticalAlignment="Top" 
                                              
                                              Content="{TemplateBinding Header}" 
                                              DockPanel.Dock="Right" Focusable="False"
                                              FontFamily="{TemplateBinding FontFamily}" 
                                              FontSize="{TemplateBinding FontSize}" 
                                              FontStretch="{TemplateBinding FontStretch}" 
                                              FontStyle="{TemplateBinding FontStyle}" 
                                              FontWeight="{TemplateBinding FontWeight}" 
                                              Foreground="{TemplateBinding Foreground}" 
                                              IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                              Padding="0" Margin="0" Style="{StaticResource ExpanderRightToggleButton}" />
                            <Border BorderThickness="1 1 0 1" BorderBrush="#FFCBCBCB">
                                <ContentPresenter x:Name="ExpandSite"  Margin="0" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Focusable="False" Visibility="Collapsed" />
                            </Border>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpanderBottomToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Width="120" HorizontalAlignment="Left" Padding="{TemplateBinding Padding}">
                        <Grid SnapsToDevicePixels="False">
                            <Grid.Background>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource Background2}" />
                            </Grid.Background>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="ContentPresenter" Grid.Column="1" Margin="5,2,5,2" VerticalAlignment="Top" SnapsToDevicePixels="True" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MessageExpander" TargetType="{x:Type Expander}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="true">
                        <DockPanel>
                            <ToggleButton VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}" ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}" DockPanel.Dock="Top" Focusable="False" FontFamily="Arial" FontSize="{TemplateBinding FontSize}" FontStretch="{TemplateBinding FontStretch}" FontStyle="{TemplateBinding FontStyle}" FontWeight="{TemplateBinding FontWeight}" IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Padding="{TemplateBinding Padding}" Style="{StaticResource ExpanderBottomToggleButton}" />
                            <ContentPresenter x:Name="ExpandSite" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" DockPanel.Dock="Right" Focusable="False" Visibility="Collapsed" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Brushes-->

    <LinearGradientBrush x:Key="BackgroundBrush">
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="0"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionColor}" Offset="1.5"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TextboxBackBrush">
        <GradientStop Color="{x:Static SystemColors.WindowColor}" Offset="0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="MenuBorderBrush" StartPoint="1,0" EndPoint="0,0">
        <GradientStop Color="White" Offset="1"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionColor}" Offset="0"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="BorderBackgroundBrush" StartPoint="1,1" EndPoint="0,0">
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="1.2"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionColor}" Offset="0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="StackPanelBackground" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="White" Offset="0"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="seperatorBrush">
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TopLabelBackground">
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="2"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ButtonBackgroundBrush" EndPoint="1,0" StartPoint="0,0">
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="0"/>
        <GradientStop Color="{x:Static SystemColors.InactiveCaptionColor}" Offset="2.3"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="BlackBackGroundStyle" EndPoint="0.5,1" StartPoint="0.5,0">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#FF00030B" />
            <GradientStop Offset="0.13" Color="#FFB5B8C0" />
            <GradientStop Offset="0.47" Color="#FF2E2F33" />
            <GradientStop Offset="0.458" Color="#FF2E3033" />
            <GradientStop Offset="0.715" Color="#FF4B5156" />
            <GradientStop Offset="0.85" Color="#FF52585C" />
            <GradientStop Offset="0.931" Color="#FF171819" />
            <GradientStop Offset="1" Color="Black" />
            <GradientStop Color="#FFE7EDF3" Offset="0.989"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="MenuItemImageColor" StartPoint="1,0" EndPoint="0,0">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="White" Offset="1"/>
                <GradientStop Color="{x:Static SystemColors.InactiveCaptionTextColor}" Offset="0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="LightBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#C7C7C7" Offset="0.0"/>
                <GradientStop Color="#DEDEDE" Offset="0.5"/>
                <GradientStop Color="#E0E0E0" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="NormalBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#CCC" Offset="0.999"/>
                <GradientStop Color="#444" Offset="0.1"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="NormalBrush" StartPoint="0.2,0.1" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#FFF" Offset="0.0"/>
                <GradientStop Color="#CCC" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="GlyphBrush" Color="#444" />
    <SolidColorBrush x:Key="SolidBorderBrush" Color="#888" />
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F0F0F0" />

    <LinearGradientBrush x:Key="DarkBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#FFF" Offset="0.0"/>
                <GradientStop Color="#AAA" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PressedBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#BBB" Offset="0.0"/>
                <GradientStop Color="#EEE" Offset="0.1"/>
                <GradientStop Color="#EEE" Offset="0.9"/>
                <GradientStop Color="#FFF" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#888" />

    <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#EEE" />

    <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="#DDD" />

    <LinearGradientBrush x:Key="HorizontalNormalBorderBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#CCC" Offset="0.0"/>
                <GradientStop Color="#444" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="DefaultedBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#777" Offset="0.0"/>
                <GradientStop Color="#000" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PressedBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#444" Offset="0.0"/>
                <GradientStop Color="#888" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="DisabledBorderBrush" Color="#AAA" />

    <SolidColorBrush x:Key="LightBorderBrush" Color="#AAA" />

    <SolidColorBrush x:Key="LightColorBrush" Color="#DDD" />

    <!-- SimpleStyles: Expander -->
    <ControlTemplate x:Key="ExpanderToggleButton" TargetType="{x:Type ToggleButton}">
        <Border
      Name="Border" 
      CornerRadius="2,0,0,0"
      Background="Transparent"
      BorderBrush="{StaticResource NormalBorderBrush}"
      BorderThickness="0,0,1,0">
            <Path 
        Name="Arrow"
        Fill="{StaticResource GlyphBrush}"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Data="M 0 0 L 4 4 L 8 0 Z"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="true">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource PressedBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
                <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type Expander}" x:Key="IncompleteExpander">
        <Setter Property="Foreground" Value="#F4F4F4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontFamily" Value="Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Name="ContentRow" Height="0"/>
                        </Grid.RowDefinitions>
                        <Border 
              Name="Border" 
              Grid.Row="0"               
              BorderThickness="0"               
              CornerRadius="0,0,0,0"
              Background="Red" >

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ToggleButton IsChecked="{Binding Path=IsExpanded,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" OverridesDefaultStyle="True" 
                  Template="{StaticResource ExpanderToggleButton}" 
                  Background="{StaticResource NormalBrush}" />
                                <ContentPresenter Grid.Column="1" Margin="4" ContentSource="Header" RecognizesAccessKey="True" />
                            </Grid>
                        </Border>
                        <Border 
              Name="Content" 
              Grid.Row="1" 
              Background="#FFFFFF"
              BorderBrush="{StaticResource SolidBorderBrush}" 
              BorderThickness="1" 
              CornerRadius="0,0,2,2" >
                            <ContentPresenter Margin="0,0,0,0" VerticalAlignment="Top" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentRow" Property="Height"
                      Value="{Binding ElementName=Content,Path=DesiredHeight}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Expander}" x:Key="CompleteExpander">
        <Setter Property="Foreground" Value="#585858"/>

        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontFamily" Value="Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Name="ContentRow" Height="0"/>
                        </Grid.RowDefinitions>
                        <Border 
              Name="Border" 
              Grid.Row="0"               
              BorderThickness="1"               
              CornerRadius="0,0,0,0"              
              BorderBrush="{StaticResource SolidBorderBrush}" 
              Background="{StaticResource NormalBrush}" >

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ToggleButton IsChecked="{Binding Path=IsExpanded,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" OverridesDefaultStyle="True" 
                  Template="{StaticResource ExpanderToggleButton}" 
                  Background="{StaticResource NormalBrush}" />
                                <ContentPresenter Grid.Column="1" Margin="4" ContentSource="Header" RecognizesAccessKey="True" />
                            </Grid>
                        </Border>
                        <Border 
              Name="Content" 
              Grid.Row="1" 
              Background="#FFFFFF"
              BorderBrush="{StaticResource SolidBorderBrush}" 
              BorderThickness="1" 
              CornerRadius="0,0,2,2" >
                            <ContentPresenter Margin="0,0,0,0" VerticalAlignment="Top" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentRow" Property="Height"
                      Value="{Binding ElementName=Content,Path=DesiredHeight}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background"
                      Value="{StaticResource DisabledBackgroundBrush}" />
                            <Setter TargetName="Border" Property="BorderBrush"
                      Value="{StaticResource DisabledBorderBrush}" />
                            <Setter Property="Foreground"
                      Value="{StaticResource DisabledForegroundBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Horizontal Expander section-->
    <Style x:Key="HorizontalGroupBoxExpanderToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid SnapsToDevicePixels="False" Background="Transparent">
                        <Ellipse HorizontalAlignment="Center" x:Name="circle" VerticalAlignment="Center" Width="15" Height="15" Fill="{DynamicResource ButtonNormalBackgroundFill}" Stroke="DarkGray"/>
                        <Ellipse Visibility="Hidden" HorizontalAlignment="Center" x:Name="shadow" VerticalAlignment="Center" Width="13" Height="13" Fill="{DynamicResource ExpanderShadowFill}"/>
                        <Path SnapsToDevicePixels="false" x:Name="arrow" VerticalAlignment="Center" HorizontalAlignment="Center" Stroke="#666" StrokeThickness="2" Data="M1,1 L4,4 1,7" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter Property="Data" TargetName="arrow" Value="M 4,1  L 1,4  L 4,7"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Stroke" TargetName="circle" Value="#666"/>
                            <Setter Property="Stroke" TargetName="arrow" Value="#222"/>
                            <Setter Property="Visibility" TargetName="shadow" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ExpanderBackground" Color="{Binding Path=Background,ElementName=ExpandSite}" />

    <SolidColorBrush x:Key="ExpanderHeader" Color="{Binding Path=Background,ElementName=ExpandSite}">
    </SolidColorBrush>

    <Style x:Key="HorizontalExpander" TargetType="{x:Type Expander}">
        <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrush}" />
        <!--<Setter Property="Margin" Value="-1,0,-2,0" />-->
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border Background="WhiteSmoke" Height="200"  CornerRadius="3" BorderThickness="1" BorderBrush="Black"  HorizontalAlignment="Left">
                        <!--<Border.Background>
                            <VisualBrush Visual="{StaticResource LeftPanelImg}">
                            </VisualBrush>
                        </Border.Background>-->
                        <Grid SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0" x:Name="LeftCol" />
                                <ColumnDefinition Width="35" x:Name="RightCol" />
                            </Grid.ColumnDefinitions>

                            <Border Height="25"  CornerRadius="3" x:Name="Header" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" BorderThickness="1"  >
                                <!--BorderBrush="{TemplateBinding BorderBrush}"-->
                                <!--<Border.Background> 
                                    <VisualBrush Visual="{StaticResource RightPanelImg}">
                                    </VisualBrush>
                                </Border.Background>-->
                            </Border>

                            <ContentPresenter x:Name="hdrPresenter" Grid.Row="1" Grid.Column="1" ContentSource="Header" RecognizesAccessKey="true"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0" Width="Auto">
                                <ContentPresenter.LayoutTransform>
                                    <RotateTransform Angle="0"  />
                                </ContentPresenter.LayoutTransform>
                            </ContentPresenter>
                            <ToggleButton Grid.Row="0" Grid.Column="1" MinHeight="0" MinWidth="0" HorizontalAlignment="Right" Margin="8"
                                        x:Name="HeaderToggle" Style="{StaticResource HorizontalGroupBoxExpanderToggleButtonStyle}"
                                        IsChecked="{Binding Path=IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" >
                            </ToggleButton>

                            <ContentPresenter x:Name="ExpandSite" Visibility="Collapsed" Grid.Row="1" Grid.Column="0" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter Property="Visibility" TargetName="ExpandSite" Value="Visible"/>
                            <Setter Property="Grid.Row" TargetName="hdrPresenter" Value="0"/>
                            <Setter Property="Grid.Column" TargetName="hdrPresenter" Value="0"/>
                            <Setter Property="Margin" TargetName="hdrPresenter" Value="1,0,1,0"/>
                            <Setter Property="HorizontalAlignment" TargetName="hdrPresenter" Value="Left"/>
                            <Setter Property="Grid.Row" TargetName="ExpandSite" Value="1"/>
                            <Setter Property="Grid.ColumnSpan" TargetName="ExpandSite" Value="2"/>
                            <Setter Property="Width" TargetName="LeftCol" Value="*"/>
                            <Setter Property="ContentPresenter.RenderTransform" TargetName="hdrPresenter">
                                <Setter.Value>
                                    <RotateTransform Angle="0" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>

                        </Trigger>
                        <Trigger Property="IsExpanded" Value="false">
                            <Setter Property="ContentPresenter.LayoutTransform" TargetName="hdrPresenter">
                                <Setter.Value>
                                    <RotateTransform Angle="270" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>