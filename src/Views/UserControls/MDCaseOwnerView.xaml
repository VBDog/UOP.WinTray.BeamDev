<UserControl x:Class="UOP.WinTray.UI.Views.MDCaseOwnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:CC="clr-namespace:UOP.WinTray.UI.CustomControls"
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views"
             xmlns:views="clr-namespace:UOP.WinTray.UI.Views"
             xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MDCaseOwnerViewModel}"
             mc:Ignorable="d" 
  
             >

    <UserControl.Resources>
        <Style x:Key="propcontrol" TargetType="{x:Type views:PropertyControl}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value= "Left" />
            <Setter Property="Margin" Value="5,0,5,0" />
            <Setter Property="Visibility" Value="{Binding Visibility}" />
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <!--<EventSetter Event="MouseDoubleClick" Handler="PropertyControl_MouseDoubleClick"/>
            <EventSetter Event="GotFocus" Handler="PropertyControl_GotFocus" />-->
        </Style>

    </UserControl.Resources>

    <Border BorderBrush="Black" x:Name="Frame" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        

        <Grid x:Name="gridDistribProps"  Margin="5,5,5,0" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Visibility="Visible" Height="{Binding ElementName=Frame, Path=Height}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

            <views:PropertyControl DataContext="{Binding Description}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding NozzleLabel}" Grid.Row="0" Grid.Column="1" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding TrayAbove}" Grid.Row="1" Grid.Column="0" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding TrayBelow}"  Grid.Row="1" Grid.Column="1" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding DistributorRequired}"  Grid.Row="2" Grid.Column="0" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding MinimizePressureDrop}"  Grid.Row="2" Grid.Column="1" Style="{StaticResource propcontrol}"/>
            <views:PropertyControl DataContext="{Binding ContainsHFTubes}"  Grid.Row="3" Grid.Column="0" Style="{StaticResource propcontrol}"/>
        </Grid>

       
       
    </Border>
</UserControl>
