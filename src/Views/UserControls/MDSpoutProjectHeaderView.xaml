<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.MDSpoutProjectHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:conv="clr-namespace:UOP.WinTray.UI.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls"
             mc:Ignorable="d">
    <UserControl.Resources>      
        <Style x:Key="LabelStyle" TargetType="Label" BasedOn="{StaticResource LabelStyle}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource WrapButton}" x:Key="WrapButton">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxStyle}">
            <Setter Property="Width" Value="150"/>
        </Style>
    </UserControl.Resources>
    
        <Grid Margin="2">
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        <ComboBox Style="{StaticResource ComboBoxStyle}" DisplayMemberPath="Name" ItemsSource="{Binding Path=RangeData}"
                   SelectedIndex="{Binding Path=SelectedRangeIndex, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  
                   SelectedItem="{Binding Path=SelectedRange, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                   Grid.Row="0" Grid.Column="0" />
    </Grid>
   

</UserControl>
