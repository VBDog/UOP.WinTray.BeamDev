<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Shared.YValuesEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="5,0,1,0">Enter Y Values: </Label>
        <TextBox Name="yValueTextBox" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding SelectedYValue}" Margin="1"/>
        <Label Grid.Row="1" Grid.Column="2" Margin="1">in</Label>
        <ListBox Name="yValuesListBox" Grid.Row="2" Grid.Column="1" SelectionMode="Single" ItemsSource="{Binding YValues}" SelectedItem="{Binding SelectedYValue}" Margin="1"/>
        <CheckBox x:Name="addMirrorValuesCheckBox" Grid.Row="3" Grid.Column="1" Content="Add Mirror Values?" VerticalContentAlignment="Center" IsChecked="{Binding AddMirrorValues}" Margin="1"/>
    </Grid>
</UserControl>
