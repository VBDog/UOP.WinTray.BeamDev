<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Shared.TextReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Name="descriptionLabel" Grid.Column="0" VerticalContentAlignment="Center" Content="{Binding Label}"/>
        <TextBox Name="valueTextBox" Grid.Column="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding Text}"/>
        <Label Name="unitLabel" Grid.Column="2" VerticalContentAlignment="Center" Content="{Binding Unit}"/>
    </Grid>
</UserControl>
