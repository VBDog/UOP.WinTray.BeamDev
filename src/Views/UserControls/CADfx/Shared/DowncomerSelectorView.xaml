<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Shared.DowncomerSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <Label Content="Select Downcomers:" HorizontalAlignment="Stretch" Margin="2" />
        <ListBox x:Name="downcomersListBox" HorizontalAlignment="Stretch" Margin="2" ItemsSource="{Binding Downcomers}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <CheckBox Content="{Binding Text}" IsChecked="{Binding IsDowncomerChecked}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>
