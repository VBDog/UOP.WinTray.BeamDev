<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Questions.NumericListQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Questions"
            xmlns:questions="clr-namespace:UOP.WinTray.UI.ViewModels.UserControls.CADfx.Questions"
            d:DataContext="{d:DesignInstance Type=questions:NumericListQuestionViewModel}"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="15,5" ToolTip="{Binding ToolTip, Mode=OneWay}">
        <Label Content="{Binding LabelText}"></Label>
        <StackPanel Orientation="Horizontal" Margin="15,0,0,0">
            <TextBox x:Name="textBox" Text="{Binding TextBoxText, UpdateSourceTrigger=PropertyChanged}" Width="100" KeyDown="textBox_KeyDown" ></TextBox>
            <Label Content="{Binding UnitLabelText}" Margin="5,0,0,0"></Label>
        </StackPanel>
        <ListBox 
            x:Name="listBox" 
            ItemsSource="{Binding Numbers}" 
            SelectedIndex="{Binding SelectedIndex}" 
            Width="100" Margin="15,0,0,0" 
            HorizontalAlignment="Left"
            MinHeight="100"
            KeyDown="listBox_KeyDown"
            >
            
        </ListBox>
        <CheckBox IsChecked="{Binding CheckBoxIsChecked}" Visibility="{Binding CheckBoxVisibility}" Margin="15,0,0,0">Add Mirror Values?</CheckBox>
        <StackPanel Orientation="Horizontal" Margin="15,10,0,10" Height="40">
            <Button Name="addButton" Margin="0,5,10,5" Padding="10" HorizontalAlignment="Left" Click="addButton_Click" Width="38">Add</Button>
            <Button Name="removeButton" Margin="0,5,10,5" Padding="10" HorizontalAlignment="Right" Click="removeButton_Click" Width="61">Remove</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
