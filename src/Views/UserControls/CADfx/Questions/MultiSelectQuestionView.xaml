<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Questions.MultiSelectQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Questions"
             xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels.UserControls.CADfx.Questions"
              d:DataContext="{d:DesignInstance Type=viewmodels:MultiSelectQuestionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MinWidth="200"
             Loaded="UserControl_Loaded"
             >
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Margin="15,5" ToolTip="{Binding ToolTip, Mode=OneWay}">
        <Label Content="{Binding LabelText}"></Label>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ListBox x:Name="ListViewInstance" HorizontalAlignment="Stretch" Margin="5" ItemsSource="{Binding Items}" VerticalAlignment="Stretch" MinHeight="60" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel >
                            <CheckBox Content="{Binding Text}" IsChecked="{Binding IsChecked}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!--<ListView x:Name="ListViewInstance" View="{Binding GridViewElement}" ItemsSource="{Binding Items}"  SelectionMode="Extended" >
                
            --><!--<ListView x:Name="ListViewInstance" ItemsSource="{Binding Items}" SelectionMode="Extended" >--><!--
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                    </Style>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Style>
                </ListView.Resources>
            
            </ListView>-->

            <!--<DataGrid x:Name="DGrid" ItemsSource="{Binding Items}" AutoGenerateColumns="True" AutoGeneratingColumn="DGrid_AutoGeneratingColumn" EnableColumnVirtualization="True" EnableRowVirtualization="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>-->
        </ScrollViewer>
    </StackPanel>
</UserControl>
