<UserControl x:Class="UOP.WinTray.UI.Views.UserControls.CADfx.Questions.DualStringChoiceQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UOP.WinTray.UI.Views.UserControls.CADfx.Questions"
            xmlns:questions="clr-namespace:UOP.WinTray.UI.ViewModels.UserControls.CADfx.Questions"
            d:DataContext="{d:DesignInstance Type=questions:DualStringChoiceQuestionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MinWidth="300"
             Loaded="UserControl_Loaded"
             >
    <Grid HorizontalAlignment="Stretch" Margin="15,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Stretch" Margin="25,0,5,0" ToolTip="{Binding ToolTip, Mode=OneWay}">
            <Label Content="{Binding LeftLabelText}"></Label>
            <ListBox x:Name="leftListBox" ItemsSource="{Binding LeftList}" SelectedIndex="{Binding LeftListSelectedIndex}" MinWidth="200" ></ListBox>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Stretch" Margin="5,0,25,0">
            <Label Content="{Binding RightLabelText}"></Label>
            <ListBox x:Name="rightListBox" ItemsSource="{Binding RightList}" SelectedIndex="{Binding RightListSelectedIndex}" MinWidth="200" ToolTip="{Binding ToolTip, Mode=OneWay}"></ListBox>
        </StackPanel>
    </Grid>
</UserControl>
