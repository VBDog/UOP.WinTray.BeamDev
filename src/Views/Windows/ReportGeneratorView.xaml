<Window x:Class="UOP.WinTray.UI.Views.Windows.ReportGeneratorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views.Windows"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        xmlns:conv="clr-namespace:UOP.WinTray.UI.Converters" 
        mc:Ignorable="d"
        MaxHeight="{x:Static SystemParameters.MaximizedPrimaryScreenHeight}"
        SizeToContent="Height" Width="800"
        xmlns:waitIndicator="clr-namespace:UOP.WinTray.UI.CustomControls.AdornerControl"
        md:DialogServiceViews.IsRegistered="True"
        xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:ReportGeneratorViewModel}"
        Title="Report Generator" >
    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="visibilityConverter" ></conv:BooleanToVisibilityConverter>
        <Style TargetType="Button" BasedOn="{StaticResource WrapButton}">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5,10"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}">
            <Setter Property="Width" Value="100"/>
        </Style>
        <Style TargetType="Label" BasedOn="{StaticResource LabelStyle}">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Width="Auto">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="25*"/>
                        <ColumnDefinition Width="173*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Folder: " HorizontalAlignment="Right" Grid.Row="0" Grid.Column="0" />
                    <Label Content="{Binding FolderName}" Grid.Column="1" FontStyle="Normal" FontSize="12" HorizontalAlignment="Stretch"  />
                    <!--<TextBox Text="{Binding FolderName}" Grid.Row="0" Grid.Column="1" MinWidth="700" HorizontalAlignment="Left" HorizontalContentAlignment="Left"  IsReadOnly="True"/>-->
                    <Label Content="File Name: " HorizontalAlignment="Right" Grid.Row="1" Grid.Column="0" />
                    <!--<Label Content="{Binding FolderName}" Grid.Column="1" FontStyle="Normal" FontSize="10" HorizontalAlignment="Left" MinWidth="700" />-->
                    <Label Content="{Binding FileName}"  Grid.Row="1" Grid.Column="1" FontStyle="Normal" FontSize="12" HorizontalAlignment="Stretch"  />
                    <!--<TextBox Text="{Binding FileName}" Grid.Row="1" Grid.Column="1" Width="350" HorizontalAlignment="Left" HorizontalContentAlignment="Left"  IsReadOnly="True"/>-->
                    <Label Content="Template Name: " HorizontalAlignment="Right" Grid.Row="2" Grid.Column="0" />
                         <Label Content="{Binding TemplateName}"  Grid.Row="2" Grid.Column="1" FontStyle="Normal" FontSize="12" HorizontalAlignment="Stretch"  />
                 
                </Grid>

                <!--<StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">

                <Label Content="Folder: " HorizontalAlignment="Right"/>
                -->
                <!--<Label Content="{Binding FolderName}" Grid.Column="1" FontStyle="Normal" FontSize="10" HorizontalAlignment="Left" MinWidth="700" />-->
                <!--
                <TextBox Text="{Binding FolderName}" Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" MinWidth="700" IsReadOnly="True"/>
               
               
            </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Stretch">
                <Label Content="File Name: "></Label>
                -->
                <!--<TextBox Width="300" Text="{Binding FileName}" HorizontalContentAlignment="Left"  IsReadOnly="True"/>-->
                <!--
                    <TextBox Text="{Binding FileName}" Grid.Column="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" MinWidth="400" IsReadOnly="True"/>
               
            </StackPanel>-->
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Report Type:" HorizontalAlignment="Left"/>
                    <Label Content="Report Pages:" Grid.Column="1" HorizontalAlignment="Left"/>
                    <Label Content="Tray Sections:" Grid.Column="2" HorizontalAlignment="Left"/>
                    <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding ReportsList}" SelectedItem="{Binding ReportSelectedItem}">

                    </ListBox>
                    <ListBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding PagesList}" SelectionMode="Multiple" IsEnabled="{Binding AllowPageSelection}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding Text}"></CheckBox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>


                    </ListBox>
                    <ListBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding TrayRangeList}" IsEnabled="{Binding AllowRangeSelection}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding Text}"></CheckBox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <CheckBox Content="Mark Revisions" Grid.Row="2" Grid.Column="0" Margin="10" IsChecked="{Binding IsMarkRevisions}" Visibility="{Binding IsMarkRevisionsVisible,Converter={StaticResource visibilityConverter}}"/>
                    <CheckBox Command="{Binding Command_CheckBoxPages}" Content="Select All Pages" Grid.Row="2" Grid.Column="1" Margin="10" IsChecked="{Binding IsSelectAllPages,Mode=TwoWay}" IsEnabled="{Binding AllowPageSelection}" Visibility="{Binding Visibility_PageSelect}" />
                    <CheckBox Command="{Binding Command_CheckBoxTrays}" Content="Select All Trays" Grid.Row="2" Grid.Column="2" Margin="10" IsChecked="{Binding IsSelectAllTrays}" IsEnabled="{Binding AllowRangeSelection}" Visibility="{Binding Visibility_RangeSelect}"  />
                </Grid>
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0">
                    <Label Content="Number of Column Sketch Pages: " Visibility="{Binding IsColumSketchCountVisible, Converter={StaticResource visibilityConverter}}" />
                    <TextBox Width="30" MaxLength="2" Text="{Binding SketchCount}" Visibility="{Binding IsColumSketchCountVisible, Converter={StaticResource visibilityConverter}}"></TextBox>
                    <StackPanel Orientation="Horizontal" Visibility="Collapsed">
                        <Label Content="Units:" Margin="100,0,0,0"></Label>
                        <RadioButton Content="English" VerticalAlignment="Center" Margin="10.0" IsChecked="{Binding IsEnglishSelected}"></RadioButton>
                        <RadioButton Content="Metric" VerticalAlignment="Center" Margin="10.0"></RadioButton>
                    </StackPanel>
                </StackPanel  >

                <Button Grid.Row="4" Style="{StaticResource GlobalButtonStyle}"  Content="Cancel"  Command="{Binding Command_Cancel}" HorizontalAlignment="Left" Margin="50,0,50,20" IsCancel="True" />
                <Button Grid.Row="4" Style="{StaticResource GlobalButtonStyle}"   Content="Execute" HorizontalAlignment="Right" Margin="50,0,50,20" Command="{Binding Command_Ok}" IsDefault="True"/>

            </Grid>
        </ScrollViewer>
</Window>
