<Window x:Class="UOP.WinTray.UI.Views.Windows.Edit_Notes_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views.Windows"
        mc:Ignorable="d"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
          xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:Edit_Notes_ViewModel}"
        md:DialogServiceViews.IsRegistered="True" MinHeight="300"
        Title="Edit Project Notes" Width="800" SizeToContent="Height" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="notes" SelectedItem="{Binding SelectedItem,UpdateSourceTrigger=PropertyChanged}" CanUserAddRows="False" Grid.Row="0" Height="300" ItemsSource="{Binding NotesCollection,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Single" AutoGenerateColumns = "False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="No" Width="auto"  Binding="{Binding NoteNo}"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Note}" Width="*" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="ToolTip" Value="{Binding Note}" />
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Cancel" Style="{StaticResource WrapButton}" Command="{Binding Command_Cancel}" IsCancel="True"/>
            <Button Content="Delete" Style="{StaticResource WrapButton}" Command="{Binding Command_Delete}"/>
            <Button Content="Add" Style="{StaticResource WrapButton}" Command="{Binding Command_Add}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <Button Content="Save" Style="{StaticResource WrapButton}" Command="{Binding Command_Save}" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
