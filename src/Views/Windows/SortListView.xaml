<Window x:Class="UOP.WinTray.UI.Views.SortListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        md:DialogServiceViews.IsRegistered="True"
        xmlns:autolocator ="clr-namespace:UOP.WinTray.UI"
        autolocator:ViewModelLocator.IsAutomaticLocator="True"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:core="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views"
          xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:SortListViewModel}"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="350" Width="400"
        Style="{StaticResource PopUpWindowTheme}" WindowStartupLocation="CenterOwner"
        >
    <Window.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource LabelStyle}">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource WrapButton}">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5,10"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0">
            <ListBox x:Name="lstInput" MouseDoubleClick="lstInput_MouseDoubleClick"  ItemsSource="{Binding  InputList, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Height="200" Margin="10" SelectedIndex="{Binding InputListSelectedIndex, Mode=TwoWay}" HorizontalContentAlignment="Stretch">
                <!--<ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" HorizontalAlignment="Stretch">
                            <TextBlock.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding RelativeSource={RelativeSource AncestorType=Window},Path=DataContext.Command_InputListDoubleClick}"></MouseBinding>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>-->
            </ListBox>
            <Label Content="double click to move =>" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <ListBox  x:Name="lstOutput" MouseDoubleClick="lstOutput_MouseDoubleClick" ItemsSource="{Binding OutputList, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Height="200" Margin="10" SelectedIndex="{Binding OutputListSelectedIndex, Mode=TwoWay}" HorizontalContentAlignment="Stretch">
                <!--<ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" HorizontalAlignment="Stretch">
                            <TextBlock.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding RelativeSource={RelativeSource AncestorType=Window},Path=DataContext.Command_OutputListDoubleClick}"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>-->
            </ListBox>
            <Label Content="&lt;= double click to remove" HorizontalAlignment="Center"/>
        </StackPanel>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="20,10">
            <Button HorizontalAlignment="Left" Content="Cancel" Command="{Binding Command_Cancel}" IsCancel="True"/>
            <Button HorizontalAlignment="Right" Content="OK" Command="{Binding Command_OK}" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>