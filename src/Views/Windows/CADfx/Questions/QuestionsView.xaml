<Window x:Class="UOP.WinTray.UI.Views.Windows.CADfx.Questions.QuestionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views.Windows.CADfx.Questions"
        mc:Ignorable="d"
        Title="Questions" 
        MaxHeight="1000" 
        MinWidth="300"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        Activated="Window_Activated"
        Closed="Window_Closed"
        >
    <ScrollViewer  x:Name="mainScoller" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
    <Grid Margin="10" x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
            
        <TextBlock x:Name="Prompt"  Grid.Row="0" Margin="5" MinWidth="200" FontSize="12" Foreground="Blue"> PROMPT</TextBlock>

            <StackPanel Name="mainStackPanel"  Grid.Row="1" Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="5,10,5,0"></StackPanel>

            <Grid Grid.Row="2" Margin="5,10,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="leftButton" Grid.Column="0" Padding="10" IsCancel="True" Click="leftButton_Click">Cancel</Button>
            <Button x:Name="rightButton" Grid.Column="2" Padding="10" Click="rightButton_Click" IsDefault="True">Save</Button>
        </Grid>
    </Grid>
    </ScrollViewer>
</Window>
