<Window x:Class="UOP.WinTray.UI.Views.WarningView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views.Windows"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
          xmlns:viewmodels="clr-namespace:UOP.WinTray.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:WarningViewModel}"
        md:DialogServiceViews.IsRegistered="True"
        mc:Ignorable="d" SizeToContent="Height" Width="800"
        MaxHeight="{x:Static SystemParameters.MaximizedPrimaryScreenHeight}"
        Title="Warnings">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView x:Name="TrayRange" Grid.Row="0" ItemsSource="{Binding WarningDocuments}" SelectedItem="{Binding SelectedItem}" Height="250">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Width="Auto" Header="Tray Section" HeaderTemplate="{StaticResource Header}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="/UOP.WinTray.UI;component/Utilities/Images/warning_icon.png" Width="12" Margin="0,0,10,0"></Image>
                                        <TextBlock>
                                            <Run Text="{Binding RangeName}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="Auto" Header="Source" HeaderTemplate="{StaticResource Header}" CellTemplate="{StaticResource Value}" DisplayMemberBinding="{Binding Owner}"/>
                        <GridViewColumn Width="Auto" Header="Brief" HeaderTemplate="{StaticResource Header}" CellTemplate="{StaticResource Value}" DisplayMemberBinding="{Binding Brief}"/>
                        <GridViewColumn Width="Auto" Header="Text" HeaderTemplate="{StaticResource Header}" CellTemplate="{StaticResource Value}" DisplayMemberBinding="{Binding TextString}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox Grid.Row="1" Height="80" Text="{Binding SelectedWarning}" TextWrapping="Wrap"></TextBox>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Print" Width="100" Height="40" Margin="10" Command="{Binding Command_Print}" IsDefault="True"></Button>
                <Button Content="Close" Width="100" Height="40" Margin="10" Command="{Binding Command_Close}" IsCancel="True"></Button>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>
