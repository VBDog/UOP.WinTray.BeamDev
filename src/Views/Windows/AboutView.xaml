<Window x:Class="UOP.WinTray.UI.Views.Windows.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UOP.WinTray.UI.Views.Windows"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        md:DialogServiceViews.IsRegistered="True"  
        mc:Ignorable="d"  Width="500"
        Style="{StaticResource PopUpWindowTheme}"
        WindowStartupLocation="CenterScreen"
        Title="About WinTray Application" SizeToContent="Height">  
        <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly"  Grid.Row="0"   HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <Label Content="Application:" FontSize="12"/>
                <Label Content="WinTray (An Engineered Product Design Tool)" FontSize="12"/>
            </StackPanel>
        </Viewbox>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly" Grid.Row="1"  HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <Label Content="Version:" FontSize="14"/>
                <Label Content="{Binding Version,UpdateSourceTrigger=PropertyChanged}" FontSize="12"/>
            </StackPanel>           
        </Viewbox>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly" Grid.Row="2" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <Label Content="Copyright @ 2021-2022 by UOP LLC. For use by authorized employees only" FontSize="12"/>
            </StackPanel>
        </Viewbox>
        <Viewbox Stretch="Uniform" StretchDirection="DownOnly" Grid.Row="3"  HorizontalAlignment="Center">
            <Button Content="Ok" Command="{Binding OkCommand}" Height="30" Width="35" Margin="5"/>
        </Viewbox>
        </Grid>
</Window>
